import { FormatMovieTimePipe } from './format-movie-time.pipe';

describe('FormatMovieTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMovieTimePipe();
    expect(pipe).toBeTruthy();
  });
});
